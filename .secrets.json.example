{
  "_notes": {
    "tmdb": "Get a free API v3 key from https://developer.themoviedb.org/docs/getting-started.",
    "_": "Copy this file to '.secrets.json' (without the .example) and paste your key in. The .secrets.json file is gitignored — never commit it."
  },
  "tmdb": {
    "api_key": "API Key"
  }
}
